[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"

# Next.js handles routing automatically, no manual redirects needed
# The @netlify/plugin-nextjs plugin handles Next.js optimization automatically
